function sumar (a: number,b:number): number{
    return (a+b);
}

const sumarFlecha = (a: number,b:number):number => {
    return a+b;
} 

function multiplicar(numero:number, base:number = 2, otron?:number):number {
    return numero * base;
}

const res = multiplicar(10,0);

console.log(res);
